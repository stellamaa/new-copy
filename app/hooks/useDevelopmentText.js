'use client';

import { useEffect } from 'react';

export function useDevelopmentText() {
  useEffect(() => {
    // Hook for development text functionality
    // Currently no-op, but can be extended for text-related features
  }, []);
}
