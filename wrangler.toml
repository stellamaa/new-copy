# Cloudflare Pages configuration
# Exclude cache files from deployment
[build]
  command = "npm run build"

# Exclude cache directories from build output
[build.upload]
  exclude = [
    "cache/**",
    ".next/cache/**",
    "*.pack",
    "node_modules/.cache/**"
  ]










